name: Wails Build

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: windows/amd64
            artifact: motiv2d-windows.zip
          - os: macos-latest
            platform: darwin/universal
            artifact: motiv2d-macos.zip

    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.11.0

      - name: Build Frontend
        run: |
          cd frontend
          npm install
          npm run build

      - name: Build Wails
        shell: bash
        run: |
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            # 1. FORCE THE DEPLOYMENT TARGET FOR THE LINKER
            export MACOSX_DEPLOYMENT_TARGET=14.0
            
            # 2. OVERWRITE THE PLIST MANUALLY TO ENSURE 10.13 IS GONE
            sed -i '' 's/10.13.0/14.0.0/g' build/darwin/Info.plist
            
            wails build -platform ${{ matrix.platform }} -clean -ldflags "-s -w"
          else
            wails build -platform ${{ matrix.platform }} -clean -ldflags "-s -w" -webview2 embed
          fi

      - name: MacOS Signing & Packaging
        if: matrix.os == 'macos-latest'
        run: |
          cd build/bin
          chmod +x motiv2d.app/Contents/MacOS/motiv2d
          # Deep signing is required for Tahoe to accept the new corner radius
          codesign --force --deep --sign - motiv2d.app
          zip -r ../../${{ matrix.artifact }} motiv2d.app

      - name: Windows Zip
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Compress-Archive -Path "build/bin/motiv2d.exe" -DestinationPath "./${{ matrix.artifact }}"

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}